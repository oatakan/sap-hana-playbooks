- hosts: localhost
  vars:
    pip_packages:
      - apypie
  tasks:
    - name: install pip packages
      pip:
        name: "{{ item }}"
        executable: /var/lib/awx/venv/ansible/bin/pip3
        state: latest
      loop: "{{ pip_packages }}"